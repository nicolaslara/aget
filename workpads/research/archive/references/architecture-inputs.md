## Architecture Inputs

| Topic | Path | Notes |
| --- | --- | --- |
| Session wrapper PoC spec | `workpads/research/session-wrapper-poc-spec.md` | Product and v1/v2 implementation spec for the thin-wrapper direction. |
| Session wrapper implementation plan | `workpads/research/session-wrapper-implementation-plan.md` | Milestone plan and test strategy for building the v1 wrapper. |
| MVP architecture proposal | `workpads/research/knowledge.md` §MVP Architecture Proposal (R12) | Consolidated architecture gate before implementation. |
| aget agent skill | `.cursor/skills/aget/SKILL.md` | Project-local agent-facing guide for safe `aget` fetch/auth flows, structured envelopes, explicit session login, session composition, and scoped cmux/Chrome import. |
| Mocked e2e site fixture | `tests/support/mock_site.rs`, `tests/mock_site_cli.rs` | Local-only deterministic site and CLI e2e tests for public/protected pages, cookie and localStorage auth, redirects, delayed content, session composition, scope rejection, and login bootstrap without manual credentials. |
| PoC backend adapters | `src/extraction.rs`, `src/session/chrome.rs`, `src/session/login.rs`, `src/session/agent_browser.rs`, `scripts/crawl4ai_extract.py` | Current Crawl4AI-compatible extraction and `agent-browser` browser/session capabilities inventoried in `workpads/research/knowledge.md` D48; replacement task tracked as I19. |
| OAuth-safe workflow assessment | `workpads/research/oauth-safe-browser-login-design.md`; `src/aget.rs`; `src/cli.rs`; `src/main.rs`; `tests/aget_api.rs`; `tests/session_cli/authorize.rs`; `workpads/research/knowledge.md` D49, D51, D151, D153, D154, D155; `README.md`; `.cursor/skills/aget/SKILL.md`; `.opencode/tools/aget.ts` | Current product supports import-first OAuth guidance, scoped Chrome import, and first-class `aget session authorize` orchestration. I20 has an explicit decision tree, vocabulary, proposed orchestration surface, browser-choice boundaries, lock/error wording, deterministic mocked-test plan, and manual OAuth smoke recipe. I21 now has `Aget::authorize_chrome_session` plus a Chrome-first CLI/envelope surface with mocked CLI tests for verified, verification-failed, profile-lock, sensitive-content omission, and re-import-after-user-login behavior. |
| Browser-choice session import | `workpads/research/browser-choice-session-import-design.md`; `src/cli.rs`; `src/main.rs`; `tests/session_cli/imports.rs`; `workpads/research/knowledge.md` D156, D157; `README.md`; `.cursor/skills/aget/SKILL.md`; `.opencode/tools/aget.ts` | I22 completed with conservative browser-choice terminology. `session import browser --browser chrome` maps to the verified Chrome import path; `chromium`, `brave`, `edge`, `arc`, `firefox`, and `safari` parse but return `usage_error` before backend access until source-specific import behavior is verified. |
| Session import test decomposition | `tests/session_cli.rs`; `tests/session_cli/imports.rs`; `tests/session_cli/imports_cmux.rs`; `workpads/research/knowledge.md` D166 | Follow-up decomposition split cmux import tests and ignored real-cmux checks out of the Chrome/browser-profile import test module, keeping backend-specific session import coverage easier to load independently. |
| CLI command decomposition | `src/cli.rs`; `src/cli/get.rs`; `src/cli/session.rs`; `src/cli/tests.rs`; `workpads/research/knowledge.md` D161 | Follow-up decomposition split get-command arguments, session/browser/import/login command arguments, and parser tests out of the top-level CLI parser module without changing public command types, flag names, defaults, or crate-level exports. |
| Binary session command decomposition | `src/main.rs`; `src/main_session.rs`; `workpads/research/knowledge.md` D162 | Follow-up decomposition moved session subcommand execution, browser/profile CLI resolution, session inspection redaction views, and login/import/authorize human output out of the binary entrypoint while preserving the same command behavior. |
| CDP discovery test decomposition | `src/browser_cdp/tests.rs`; `src/browser_cdp/tests/discovery.rs`; `workpads/research/knowledge.md` D167 | Follow-up decomposition split CDP discovery/startup diagnostic unit tests out of the root CDP test module, matching the production `src/browser_cdp/discovery.rs` boundary. |
| Mock-site AgetExtractor fixture decomposition | `tests/mock_site_cli.rs`; `tests/mock_site_cli/aget_extractor.rs`; `tests/mock_site_cli/aget_extractor_site.rs`; `workpads/research/knowledge.md` D165 | Follow-up decomposition moved the AgetExtractor mock route corpus out of the AgetExtractor parity assertion file so future extraction test edits can load the fixture and assertions separately. |
| Owned semantic markdown block boundaries | `references/repos/crawl4ai/crawl4ai/content_filter_strategy.py`; `references/repos/crawl4ai/crawl4ai/html2text/__init__.py`; `src/extraction/markdown.rs`; `tests/mock_site_cli/aget_extractor.rs`; `workpads/research/knowledge.md` D163 | I19d source inspection mapped Crawl4AI's semantic content tags and CustomHTML2Text fallback handling. `aget` now gives figure, figcaption, details, summary, and address stable block boundaries in owned markdown output. |
| Owned automatic markdown link titles | `references/repos/crawl4ai/crawl4ai/html2text/__init__.py`; `src/extraction/markdown.rs`; `tests/mock_site_cli/aget_extractor.rs`; `workpads/research/knowledge.md` D164 | I19d source inspection mapped Crawl4AI/html2text's automatic absolute-link edge case. `aget` now keeps `<https://...>` automatic links title-insensitive while preserving titles on ordinary links. |
| Owned main-content candidate selection | `references/repos/crawl4ai/crawl4ai/content_filter_strategy.py`; `src/extraction/owned.rs`; `tests/mock_site_cli/aget_extractor.rs`; `workpads/research/knowledge.md` D158, D160 | I19d source inspection used Crawl4AI's semantic include/exclude tags, negative label patterns, minimum word count, and chunk extraction as inspiration. `aget` now ranks multiple semantic main-content candidates and labeled `section`/`div` content containers with deterministic text/link/label scoring instead of falling back to the whole body when useful content is not wrapped in a unique semantic element. |
| Owned word-count pruning | `references/repos/crawl4ai/crawl4ai/content_scraping_strategy.py`; `references/repos/crawl4ai/crawl4ai/utils.py`; `src/extraction/html_clean.rs`; `src/extraction/owned.rs`; `tests/mock_site_cli/aget_extractor.rs`; `workpads/research/knowledge.md` D159 | I19d source inspection mapped Crawl4AI's bottom-up low-word leaf pruning, bypass tags, and code-block preservation. `aget` now applies `crawl4ai.word_count_threshold` to owned cleanup instead of only validating the option. |
| Dependency migration source snapshots | `references/repos/crawl4ai`, `references/repos/agent-browser` | Both clones live under gitignored `references/repos/`. I19a captured source/test entrypoints for migration in `knowledge.md` D52. Crawl4AI paths to inspect before extraction porting include `crawl4ai/async_webcrawler.py`, `crawl4ai/async_configs.py`, `crawl4ai/browser_manager.py`, `crawl4ai/async_crawler_strategy.py`, `crawl4ai/markdown_generation_strategy.py`, `crawl4ai/content_scraping_strategy.py`, `crawl4ai/html2text/`, `tests/async/`, `tests/browser/`, and `tests/cli/`. `agent-browser` paths to inspect before browser/session porting include `cli/src/commands.rs`, `cli/src/connection.rs`, `cli/src/native/browser.rs`, `cli/src/native/state.rs`, `cli/src/native/cookies.rs`, `cli/src/native/storage.rs`, `cli/src/native/cdp/`, `cli/src/native/e2e_tests.rs`, and `cli/tests/doctor_cli.rs`. |
| Owned extractor slices | `src/extraction/mod.rs`, `src/extraction/owned.rs`, `src/extraction/markdown.rs`, `src/extraction/command.rs`, `src/extraction/fallback_command.rs`, `src/extraction/html_clean.rs`, `src/extraction/http.rs`, `src/extraction/artifacts.rs`, `src/browser_cdp.rs`, `src/browser_cdp/page_scripts.rs`, `src/browser_cdp/render.rs`, `scripts/crawl4ai_extract.py`, `tests/mock_site_cli.rs`, `tests/mock_site_cli/aget_extractor.rs`, `tests/mock_site_sessions.rs`, `tests/mock_site_docs_contract.rs`, `tests/support/mock_site.rs`, `tests/support/mock_site_cli.rs`, `tests/support/get_cli.rs`, `tests/get_cli/session.rs`, `Cargo.toml`, `workpads/research/knowledge.md` D55-D57, D59, D61-D63, D69-D73, D77-D82, D86-D95, D98-D100, D104-D115, D119-D122, D123-D128, D133, D134, D137, D140, D141, D143, D147, D148, and D149 | I19d source inspection mapped Crawl4AI's browser/navigation, HTML processing, markdown, output-selection, cleaned-content, safe option-handling, pre-HTML-return delay, image-wait, `word_count_threshold`, network-idle, selector miss behavior, invalid selector fallback, all-match selector extraction, selected-wrapper HTML preservation, cleaned-HTML tag removal, important-attribute pruning, base64 image source cleanup, empty-element pruning, generic overlay removal, rendered style/z-index/fixed/sticky overlay cleanup, shadow DOM flattening, a bounded `CustomHTML2Text` markdown tag subset, nested list rendering, link title/mailto/internal-link defaults, automatic absolute-link rendering, table captions, underscore emphasis markers, underline-as-emphasis, abbreviation definitions, unordered bullet marker, blockquote paragraph preservation, markdown hard-break handling, accidental list-marker escaping and literal backslash preservation in text, markdown construct escaping for link/image targets and titles, empty anchor-label handling, and linked-image anchors before adding `OwnedExtractorBackend` slices. The owned compatibility adapters now live in `src/extraction/command.rs` and `src/extraction/fallback_command.rs`; owned HTML cleanup now lives in `src/extraction/html_clean.rs`; owned HTTP/static fetch now lives in `src/extraction/http.rs`; extraction artifact, metadata, private-file, and redaction helpers now live in `src/extraction/artifacts.rs`; owned extraction and AgetBrowser fallback orchestration now live in `src/extraction/owned.rs`. Mock-site CLI helper setup for owned extraction integration tests now lives in `tests/support/mock_site_cli.rs`, AgetExtractor mock-site parity coverage now lives in `tests/mock_site_cli/aget_extractor.rs`, documentation contract coverage now lives in `tests/mock_site_docs_contract.rs`, mock-site session/auth coverage now lives in `tests/mock_site_sessions.rs`, shared get CLI backend/session/cookie helpers now live in `tests/support/get_cli.rs`, and session-backed get replay/fallback coverage now lives in `tests/get_cli/session.rs`. The owned backend now uses Rust HTTP(S) transport, CSS selector parsing with Crawl4AI-style no-match and invalid-selector fallback, all-match extraction, selected-wrapper HTML preservation, invalid-exclude-selector tolerance, generic overlay/modal/cookie/dialog selector cleanup, rendered style/z-index/fixed/sticky overlay cleanup before CDP `outerHTML` capture, and optional shadow DOM flattening before CDP HTML capture, with CDP page scripts now isolated in `src/browser_cdp/page_scripts.rs` and rendered-page orchestration now isolated in `src/browser_cdp/render.rs`, structural HTML-to-markdown rendering in `src/extraction/markdown.rs` including simple tables with captions, nested lists using Crawl4AI/html2text `*` unordered bullets, blockquotes that preserve child block breaks, Markdown hard breaks for `<br>`, escaped accidental list markers and literal backslashes in text, horizontal rules, definition lists, strikethrough, quoted inline text, keyboard/teletype inline code, underscore emphasis for `em`/`i`/`u`, abbreviation title definitions, link titles with escaped Markdown constructs, `mailto:` and fragment-only link suppression, automatic absolute links, empty anchor labels, linked-image anchors, escaped link/image markdown targets, and base-URL-aware markdown links, cleaned-HTML removal of script/style/link/meta/noscript plus base64 image source cleanup, empty-leaf pruning, and important-attribute pruning, CDP rendering for localStorage, CSS-waited pages, and detected script-bearing pages, conservative default main-content selection, and safe `crawl4ai.excluded_tags`/`crawl4ai.target_elements`/`crawl4ai.only_text`/`crawl4ai.word_count_threshold`/`crawl4ai.delay_before_return_html`/`crawl4ai.page_timeout`/`crawl4ai.wait_for_timeout`/`crawl4ai.wait_until` values `domcontentloaded`, `load`, and `networkidle` plus `crawl4ai.wait_for_images` and `crawl4ai.flatten_shadow_dom` through `ureq`, `scraper`, `html5ever`, `ego-tree`, `url`, and `tungstenite`; it still does not cover full Crawl4AI-quality readability/markdown or richer rendered-readiness heuristics. |
| AgetBrowser automation slices | `src/aget.rs`, `src/aget_browser.rs`, `src/browser_cdp.rs`, `src/browser_cdp/page_scripts.rs`, `src/browser_cdp/process.rs`, `src/browser_cdp/discovery.rs`, `src/browser_cdp/chrome_process.rs`, `src/browser_cdp/client.rs`, `src/browser_cdp/session_data.rs`, `src/browser_cdp/render.rs`, `src/browser_cdp/state.rs`, `src/browser_cdp/login.rs`, `src/browser_cdp/tests.rs`, `src/extraction/mod.rs`, `src/session/chrome.rs`, `src/session/login.rs`, `src/session/store.rs`, `tests/mock_site_cli.rs`, `tests/mock_site_browser.rs`, `tests/aget_api.rs`, `tests/session_cli.rs`, `tests/session_cli/imports.rs`, `tests/session_cli/login.rs`, `tests/support/session_cli.rs`, `workpads/research/knowledge.md` D58, D60, D64-D68, D83-D85, D96-D97, D101-D103, D116-D118, D126, D129, D130, D131, D132, D134, D135, D136, D138, D139, D142, D144, D145, D146, D170, and D173 | I19e source inspection mapped the current `agent-browser` fallback, state/import, and login lifecycle paths: state load/save, URL open/navigation, body HTML/text extraction, close, CDP cookie/state export, Chrome profile launch, named profile copy behavior, login start/finish/cancel, Chrome startup stderr diagnostics including stderr DevTools URL fallback, sandbox/namespace hints, and no-stderr exits, existing-profile CDP discovery fallbacks, sessionStorage replay/export, Chrome launch retries, and stale `DevToolsActivePort` cleanup. `AgetBrowserBackend` now has static and scripted cookie-backed fallback, localStorage/sessionStorage-backed CDP fallback/rendering, explicit user-data-dir Chrome import, named Chrome profile resolution/copying, orphan cleanup for stale copied profiles and unreferenced owned-login profiles, a first owned dedicated-profile login lifecycle, PID-backed cleanup for detached owned login browsers, profile-in-use Chrome startup classification as `requires_user_action`, Chrome stderr `DevTools listening on ...` URL startup fallback, sandbox/namespace startup hints, no-stderr Chrome startup hints, `/json/version`, `/json/list`, plus direct `/devtools/browser` CDP discovery fallbacks when attaching to an existing profile browser, three-attempt owned Chrome launch retries, stale `DevToolsActivePort` removal when existing-profile attach finds a dead endpoint, and direct no-Chrome engine coverage for pending-login cancellation. CDP page scripts live in `src/browser_cdp/page_scripts.rs`; process control helpers live in `src/browser_cdp/process.rs`; CDP startup/discovery helpers live in `src/browser_cdp/discovery.rs`; Chrome launch/profile process ownership lives in `src/browser_cdp/chrome_process.rs`; CDP client/session plumbing lives in `src/browser_cdp/client.rs`; CDP cookie/storage/target translation helpers live in `src/browser_cdp/session_data.rs`; rendered-page orchestration lives in `src/browser_cdp/render.rs`; Chrome profile state export orchestration lives in `src/browser_cdp/state.rs`; login start/finish/cancel orchestration lives in `src/browser_cdp/login.rs`; CDP unit tests live in `src/browser_cdp/tests.rs`; mock-site AgetBrowser/rendering integration coverage lives in `tests/mock_site_browser.rs`; shared session CLI fixture helpers live in `tests/support/session_cli.rs`; session import and real-cmux import coverage lives in `tests/session_cli/imports.rs`; session login lifecycle coverage lives in `tests/session_cli/login.rs`. Current-tab attach, broader rendered JavaScript parity, real logged-in Chrome/keychain smoke coverage, and fuller cross-platform process/error classification remain open. |
| Default local runtime switch | `src/aget.rs`, `src/extraction/mod.rs`, `tests/mock_site_cli.rs`, `README.md`, `.opencode/tools/aget.ts`, `.cursor/skills/aget/SKILL.md`, `workpads/research/knowledge.md` D74 | I19f switched default runtime wiring to AgetExtractor and AgetBrowser backends. `Aget` now defaults to AgetExtractor/AgetBrowser automation; explicit command adapters remain available when compatibility env vars are set. README, OpenCode, and the project aget skill describe Crawl4AI and `agent-browser` as optional compatibility/development backends rather than required default runtime dependencies. |
| PoC command-default demotion | `src/extraction/mod.rs`, `src/session/agent_browser.rs`, `src/cli.rs`, `README.md`, `.cursor/skills/aget/SKILL.md`, `workpads/research/knowledge.md` D75 | I19g removed the implicit `scripts/crawl4ai_extract.py`/`uv run --with crawl4ai` fallback and the implicit `agent-browser` PATH fallback from compatibility adapters. Command-backed compatibility now requires explicit `AGET_CRAWL4AI_COMMAND`, `AGET_AGENT_BROWSER_COMMAND`, or an API-provided command. |

